<?php
/**
 * FunnelKit constant stubs for PHPUnit tests.
 *
 * FunnelKit stores data in custom DB tables queried via $wpdb,
 * so no class stubs are needed — only the version constant.
 *
 * @package WP4Odoo\Tests
 */

if ( ! defined( 'WFFN_VERSION' ) ) {
	define( 'WFFN_VERSION', '3.3.0' );
}
